# 종료·방치된 방이 game_rooms에 계속 쌓임

방 상태를 `game_rooms`에 저장하면서 삭제 경로를 만들지 않았다. `finished` 상태가 된 방이나 로비에서 방치된 방이 지워지지 않고 누적된다. 강의용 규모에서는 문제되지 않지만 장기 운영에는 정리가 필요하다.

`updated_at` 인덱스는 이미 있으므로, 일정 시간(예: 24시간) 지난 행을 지우는 Supabase cron job이나 pg_cron 작업을 붙이면 된다.
